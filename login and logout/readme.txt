Steps to run the project ==>
// First go to the mail function.php file chage the email id and pass of mail sender
1)-Copy the (Web Developement Final Project)folder and paste it in the xampp (any other sql server) folder
2)-open the xampp start the server (Apache server and mySQL server both) 
3)-now open the server in browser on it automatically open in browser by some link like localhost/dashboard (in my case)
4)-Now import the sqlfile (notesdun.sql) in myphpadmin server in myphpadmin
5)-Now in PHPServer(ApacheServer) which is already opened in browser... localhost/dashboard/all/project_name
6)-the PHPServer(ApacheServer) runs the website
7)-the PHPServer(ApacheServer) opened the file index.php
